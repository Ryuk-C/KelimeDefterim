<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.haznedar.kelimedefterim">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KelimeDefterim"
        android:usesCleartextTraffic="true">
        <activity android:name=".Testler.CoktanSecmeliActivity" />
        <activity android:name=".Testler.YazmaAlistirmasiActivity" />
        <activity android:name=".Testler.AlistirmaKartlariActivity" />
        <activity android:name=".Activity.GirisActivity" />
        <activity android:name=".Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.MainActivity"
            android:windowSoftInputMode="adjustPan" />

        <receiver android:name=".Receiver.AlarmReceiver" />
    </application>

</manifest>